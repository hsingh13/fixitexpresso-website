# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
.vscode/
.idea/

# Temporary files
*.tmp
*.temp
*~

# Log files
*.log

# Node modules (if applicable)
node_modules/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Local staging-only reference material — never meant to ship, so it stays
# out of every commit and can't accidentally ride along on a merge to main.
/.staging-context/